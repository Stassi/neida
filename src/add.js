/**
 * @description Add two values.
 * @example
 * sum(2, 3)
 * // => 5
 * @module
 * @param {number} n
 * @param {number} m
 * @returns {number} sum of both values
 * @see {@link addOne}
 * @see {@link sum}
 */
export default function add (n, m) {
  return n + m
}
