/**
 * @description Add two values.
 * @example
 * sum(2, 3)
 * // => 5
 * @module
 * @param {number} x
 * @param {number} y
 * @returns {number} sum of x and y
 * @see {@link addOne}
 */
export default function add (x, y) {
  return x + y
}
